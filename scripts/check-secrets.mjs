// Placeholder per Step 24: scan di segreti e valori reali nel repository.
// In questo step esce sempre 0; lo Step 24 di STEP.md ne implementa la logica.
console.log("[check:secrets] placeholder attivo — implementazione completa nello Step 24.");
process.exit(0);
